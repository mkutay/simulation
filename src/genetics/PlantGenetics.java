package genetics;

public class PlantGenetics extends Genetics {
  public PlantGenetics(int maxAge, int matureAge, double spreadRate, double size, String name) {
    super(maxAge, matureAge, spreadRate, size, name);
  }
}
