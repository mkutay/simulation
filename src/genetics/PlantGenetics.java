package genetics;

public class PlantGenetics extends Genetics {
  public PlantGenetics(int maxAge, int matureAge, double spreadRate, double size) {
    super(maxAge, matureAge, spreadRate, size);
  }
}
